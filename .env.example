# Configuration de la base de données PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=polostore_db
DB_USER=votre_utilisateur_db
DB_PASSWORD=votre_mot_de_passe_db

# Configuration Railway (Production)
# Railway utilise automatiquement DATABASE_URL - ne pas définir manuellement
# DATABASE_URL=postgresql://user:password@host:port/database

# Configuration JWT (OBLIGATOIRE - Utilisez une clé forte de minimum 32 caractères)
JWT_SECRET=votre_cle_secrete_jwt_tres_longue_et_complexe_minimum_32_caracteres
JWT_EXPIRE=7d

# Configuration du serveur
PORT=5050
NODE_ENV=development

# URLs autorisées pour CORS (en production)
FRONTEND_URL=http://localhost:3000
PRODUCTION_URL=https://votre-domaine.com

# Configuration email (optionnel)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=votre-email@gmail.com
SMTP_PASS=votre-mot-de-passe-app

# Clés API externes (si nécessaire)
# API_KEY=votre_cle_api
